<!DOCTYPE html>
<html>
<head>
    <title>Edit Post</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Edit Blog Post</h1>

    <form action="/posts/<%= post._id %>/update" method="POST">
        <label>Title:</label><br>
        <input type="text" name="title" value="<%= post.title %>" required><br><br>

        <label>Content:</label><br>
        <textarea name="content" rows="5" cols="30" required><%= post.content %></textarea><br><br>

        <button type="submit">Update Post</button>
    </form>

    <form action="/posts/<%= post._id %>/delete" method="POST" style="margin-top: 1em;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this post?')">🗑️ Delete Post</button>
    </form>

    <p><a href="/posts/<%= post._id %>">← Cancel</a></p>
</body>
</html>
